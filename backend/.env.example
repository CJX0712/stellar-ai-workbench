# Author: 晨星
# 复制为 .env 后按需填写；不配置 API_KEY 时自动走 Mock Provider（端到端可跑通）
# 键名与 Settings 字段一一对应，大小写不敏感；POST /api/v1/settings 会回写这些键
BASE_URL=https://api.openai.com/v1
API_KEY=
MODEL=mock
FALLBACK_MODEL=
EMBEDDING_MODEL=text-embedding-3-small
DATA_DIR=data
LOG_LEVEL=INFO
HOST=127.0.0.1
PORT=8000
